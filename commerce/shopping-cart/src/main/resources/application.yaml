spring:
  application:
    name: shopping-cart
  config:
    import: "configserver:"
  cloud:
    config:
      fail-fast: true
      retry:
        useRandomPolicy: true
        max-interval: 6000
      discovery:
        enabled: true
        serviceId: config-server
  datasource:
    username: postgres
    password: 824322
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/smart-hub-shopping-cart


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/