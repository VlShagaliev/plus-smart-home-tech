server:
  port: 0

spring:
  application:
    name: payment
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
  datasource:
    username: postgres
    password: 824322
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/smart-hub-payment